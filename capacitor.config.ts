import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'limited-photos',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
